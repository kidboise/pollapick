<%= link_to 'Make a New Poll', new_poll_path, :class => "btn" %>

<% @polls.each do |poll| %>
  <div class="poll-container">
    <%= render 'poll', :poll => poll %>
  </div>
<% end %>

<%= link_to 'Logout', destroy_user_session_path, method: :delete, class: "btn" %>


