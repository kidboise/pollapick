<%= link_to 'Make a New Poll', new_poll_path, :class => "btn" %><br><br>
<% @polls.each do |poll| %>
  <div class="poll-container poll-<%= poll.id %>">
    <%= render 'shared/poll', :poll => poll %>
  </div>
<% end %>
