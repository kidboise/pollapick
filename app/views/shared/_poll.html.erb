<div class="poll" id="poll-<%= poll.id %>">
  <h2><%= poll.question %></h2>
  <div class="poll-options">
    <%= render "shared/options", poll: poll %>
  </div>
</div>

<%= render "shared/poll_votes", poll: poll %>